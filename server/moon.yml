tasks:
  dev:
    command: 'air'
    local: true
    options:
      persistent: true

  test:
    command: 'pnpm vitest run --no-color --bail=1 --fileParallelism=false'
  setup:
    command: 'pnpm install'